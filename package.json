{
  "name": "dominion-city-church",
  "version": "2.0.0",
  "description": "Dominion City Church PWA — Navasota, TX",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/ tests/",
    "lint:css": "stylelint 'src/**/*.css'",
    "lint:fix": "eslint src/ tests/ --fix && stylelint 'src/**/*.css' --fix",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@supabase/supabase-js": "^2.101.0",
    "eslint": "^10.1.0",
    "globals": "^17.4.0",
    "stylelint": "^17.6.0",
    "stylelint-config-standard": "^40.0.0",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  }
}
